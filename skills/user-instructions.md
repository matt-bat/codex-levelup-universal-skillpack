# User Instructions Ledger Compatibility Path

The canonical ledger moved to [`../user-instructions.md`](../user-instructions.md) under the authorized version 2 migration.

Do not record directives in this compatibility file. Tools must resolve the root ledger first and accept this path only as a legacy pointer during the compatibility window.
